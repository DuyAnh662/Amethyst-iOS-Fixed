name: Release build

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Release build
    strategy:
      matrix:
        include:
          - platform: 2
            platform_name: ios
    
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
        with:
          submodules: true

      - name: Fetch MobileGlues nested deps (not proper git submodules in this repo)
        run: |
          cd Natives/external/MobileGlues
          echo "=== Cloning MobileGlues 3rdparty dependencies ==="
          mkdir -p MobileGlues-cpp/3rdparty
          git clone --depth=1 https://github.com/KhronosGroup/glslang.git MobileGlues-cpp/3rdparty/glslang
          git clone --depth=1 https://github.com/KhronosGroup/SPIRV-Cross.git MobileGlues-cpp/3rdparty/SPIRV-Cross
          git clone --depth=1 https://android.googlesource.com/platform/external/perfetto MobileGlues-cpp/3rdparty/perfetto
          git clone --depth=1 https://github.com/stbrumme/xxhash.git MobileGlues-cpp/3rdparty/xxhash
          git clone --depth=1 https://github.com/g-truc/glm.git MobileGlues-cpp/3rdparty/glm
          git clone --depth=1 https://github.com/MobileGL-Dev/FastSTL.git MobileGlues-cpp/include/FastSTL
          echo "=== Verification ==="
          ls -d MobileGlues-cpp/3rdparty/*/ MobileGlues-cpp/include/FastSTL/

      - name: Download UnzipKit framework & headers
        run: |
          # Download and extract framework binary
          curl -sL "https://github.com/abbeycode/UnzipKit/releases/download/1.9/UnzipKit.framework.zip" -o /tmp/UnzipKit.framework.zip
          mkdir -p Natives/resources/Frameworks
          unzip -oq /tmp/UnzipKit.framework.zip -d Natives/resources/Frameworks/
          rm -f /tmp/UnzipKit.framework.zip
          # Headers are gitignored at Natives/external/UnzipKit, fetch from source
          UZK_COMMIT=61159ce86e02c429b790c0c45c9c0a3ea818ca39
          mkdir -p Natives/external/UnzipKit
          curl -sL "https://raw.githubusercontent.com/abbeycode/UnzipKit/$UZK_COMMIT/Source/UnzipKit.h" -o Natives/external/UnzipKit/UnzipKit.h
          curl -sL "https://raw.githubusercontent.com/abbeycode/UnzipKit/$UZK_COMMIT/Source/UZKArchive.h" -o Natives/external/UnzipKit/UZKArchive.h
          curl -sL "https://raw.githubusercontent.com/abbeycode/UnzipKit/$UZK_COMMIT/Source/UZKFileInfo.h" -o Natives/external/UnzipKit/UZKFileInfo.h
          echo "=== UnzipKit installed ==="
          ls -la Natives/external/UnzipKit/ Natives/resources/Frameworks/UnzipKit.framework/

      - name: Install dependencies
        run: |
          export HOMEBREW_NO_ENV_HINTS=1 HOMEBREW_NO_REQUIRE_TAP_TRUST=1
          command -v gmake &>/dev/null || brew install make 2>&1 | grep -v "^$"
          command -v cmake &>/dev/null || brew install cmake 2>&1 | grep -v "^$"
          command -v wget &>/dev/null || brew install wget 2>&1 | grep -v "^$"
          command -v xz &>/dev/null || brew install xz 2>&1 | grep -v "^$"
          brew install --cask zulu@8 2>&1 | grep -v "already\|trusted\|untrusted\|The following\|^$"
          brew install libplist openssl 2>&1 | grep -v "already\|trusted\|untrusted\|The following\|^$"
          command -v ldid &>/dev/null || {
            git clone --depth=1 https://github.com/ProcursusTeam/ldid.git
            cd ldid
            make -j$(sysctl -n hw.ncpu)
            sudo cp ldid /usr/local/bin/ldid
            cd ..
            rm -rf ldid
          }

      - name: Select Xcode version
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcrun --sdk iphoneos --show-sdk-path

      - name: Build for ${{ matrix.platform_name }}
        env:
          RELEASE: 1
        run: |
          export PATH=$(brew --prefix)/bin:$PATH
          export ZULU_HOME="/Library/Java/JavaVirtualMachines/zulu-8.jdk/Contents/Home"
          export BOOTJDK="$ZULU_HOME/bin"
          export JAVA_HOME="$ZULU_HOME"
          export PATH="$ZULU_HOME/bin:$(brew --prefix)/bin:$PATH"
          "$ZULU_HOME/bin/javac" -version
          gmake -j$(sysctl -n hw.ncpu) dsym package PLATFORM=${{ matrix.platform }}

      - name: Upload regular ipa
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: org.angelauramc.amethyst-${{ matrix.platform_name }}.ipa
          path: artifacts/org.angelauramc.amethyst-*-${{ matrix.platform_name }}.ipa

      - name: Upload slimmed ipa
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: org.angelauramc.amethyst.slimmed-${{ matrix.platform_name }}.ipa
          path: artifacts/org.angelauramc.amethyst.slimmed-*-${{ matrix.platform_name }}.ipa

      - name: Upload dSYM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: matrix.platform_name == 'ios'
        with:
          name: AngelAuraAmethyst.dSYM
          path: artifacts/AngelAuraAmethyst.dSYM
